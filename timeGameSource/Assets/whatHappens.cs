using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class whatHappens : MonoBehaviour
{
    /*
    What happens in this tutorial level
    1. tell player how to walk
    2. tell player to walk to the square named walkTo (it has a trigger so u can call the function)
    3. when they hit that, tell them how to jump
    4. tell them to hit the flag and that hitting the flag is how u win
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
